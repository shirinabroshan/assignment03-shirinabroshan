# assignment03-shirinabroshan// skapa ett folder i git 
npm init playwright@latest./släpp git action 
 npm install dotenv
 npm install wait-on
 vi kör koderna // npx playwright test --ui
   i config ska ändra det ----fullyParallel: false,

